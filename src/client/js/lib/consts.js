(function(global) {
    "use strict;"

    var Consts = {
        
    };

    // Exports ----------------------------------------------
    module["exports"] = Consts;

})((this || 0).self || global);