(function(global) {
    "use strict;"

    var Consts = {
        credentialsMinLength: 6
    };

    // Exports ----------------------------------------------
    module["exports"] = Consts;

})((this || 0).self || global);