(function(global) {
    "use strict;"

    var Config = {
        defaultContaier : 'body' // write JQuery style selector
    };

    // Exports ----------------------------------------------
    module["exports"] = Config;

})((this || 0).self || global);